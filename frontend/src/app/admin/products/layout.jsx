export default function ProductsLayout({ children }) {
    return (
        <body>
            <header>
                products layout
            </header>
            {children}
            <footer>
                footer
            </footer>
        </body>
    );
}