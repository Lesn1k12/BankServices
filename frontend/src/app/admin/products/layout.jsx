export default function ProductsLayout({ children }) {
    return (
      <div>
        <header>
          products layout
        </header>
        {children}
        <footer>
          footer
        </footer>
      </div>
    );
  }